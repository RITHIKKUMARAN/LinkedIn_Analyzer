{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "devCommand": "npm run dev",
    "installCommand": "npm install",
    "framework": "vite",
    "env": {
        "VITE_API_URL": {
            "description": "Backend API URL (will be Railway URL)",
            "value": "https://linkedin-insights-api.onrender.com"
        }
    }
}